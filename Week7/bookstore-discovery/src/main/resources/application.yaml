spring:
  application:
    name: bookstore-discovery-service

server:
  port: 8761 #well known port number for Eureka

eureka:
  instance:
    hostname: localhost
  client:
    register-with-eureka: false
    fetch-registry: false
  server:
    enable-self-preservation: false
